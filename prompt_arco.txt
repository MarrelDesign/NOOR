PROYECTO: "noor" (ya existe). El difusor del hero AHORA SE VE, pero está mal calibrado: (1) es GIGANTE y pegado a la cámara — llena toda la pantalla como una losa; (2) los reflejos del environment lo pusieron BLANCO/GRIS en vez de negro premium. Ajuste de calibración. NO toques copy, secciones, CTA, footer ni scroll. Confirma git limpio antes.

──────────────────────────────────────────
AJUSTES
──────────────────────────────────────────
1) ESCALA Y ENCUADRE (lo principal):
   - Reduce el tamaño del difusor y/o aleja la cámara hasta que se vea el DIFUSOR ENTERO, centrado, con AIRE alrededor (espacio vacío arriba, abajo y a los lados) — como un producto en un escaparate, NO ocupando toda la pantalla.
   - Como referencia: que el difusor ocupe aprox. el 40-55% de la altura del viewport y quede centrado (o ligeramente hacia arriba para dejar sitio al titular). Debe verse la caja completa con sus patas y la ranura superior.
   - Revisa MODEL.height y la posición/distancia de la cámara (fov y position.z). Ajusta hasta que encuadre bien en desktop.

2) MATERIAL DEL CUERPO — que vuelva a ser NEGRO PREMIUM (ahora está blanco por exceso de reflejo):
   - Baja envMapIntensity del material del cuerpo a ~0.3-0.4 (reflejos SUTILES, no espejo).
   - Cuerpo: color #1c1b1f, metalness ~0.2, roughness ~0.45. Debe verse carbón/negro oscuro con brillos cálidos suaves en los bordes, NO una losa gris/blanca.
   - Mantén el environment SOLO para reflejos (scene.environment), nunca como background (el fondo sigue #141019).

3) Comprueba que la LLAMA sigue saliendo de la ranura superior a la escala correcta del modelo ya reducido (reposiciónala/reescálala si hace falta para que sea proporcionada y salga del hueco de arriba). Pequeña y suave, sin cono ni blowout.

4) Mantén: rotación idle + scroll con damp, rim light, partículas de niebla sutiles.

──────────────────────────────────────────
RESTRICCIONES
──────────────────────────────────────────
- Vanilla JS + Vite + Three.js. Sin React. No uses el navegador para verificar — reporta y yo verifico con capturas.
- `rm -rf dist && npm run build`, reporta errores.
- Commit: "hero: calibrar escala/encuadre del difusor + reflejos sutiles (negro premium)".

REPORTA los valores finales de: MODEL.height, cámara (fov, position.z), envMapIntensity, metalness/roughness del cuerpo, y posición/escala de la llama — para poder afinar rápido luego.
