PROYECTO: "noor" (ya existe). Tarea: sustituir el difusor PLACEHOLDER del hero por el MODELO 3D REAL y darle acabado premium. No toques el copy, las secciones, el CTA ni el scroll suave. Confirma git limpio antes; si no, commitea.

El modelo ya está en /public/models/diffuser.glb (hecho por código, ~844 caras, muy ligero). Es una chimenea-difusor rectangular: cuerpo/marco oscuro, ventana frontal con cristal semitransparente (material glass con alphaMode BLEND), troncos dentro, 4 patas, y una RANURA en la cara superior (por ahí sale la niebla). El modelo mira hacia +Z (frente), Y es arriba.

──────────────────────────────────────────
QUÉ HACER
──────────────────────────────────────────
1) CARGAR EL MODELO: usa el GLTFLoader ya preparado para cargar '/public/models/diffuser.glb'. Elimina/oculta la primitiva placeholder. Centra el modelo y escálalo para que tenga presencia en el hero (que ocupe una buena porción vertical, sin cortarse). Oriéntalo mirando a cámara (frente +Z hacia el espectador), ligeramente en 3/4 para que se le vea volumen. Si el GLB no carga (404), que caiga al placeholder sin romper.

2) MATERIALES (acabado premium):
   - CUERPO/MARCO y PATAS: MeshStandardMaterial oscuro premium (color ~#1c1b1f, roughness ~0.55, metalness ~0.25). Debe captar los reflejos cálidos de la llama.
   - CRISTAL (la pieza "glass"): mantenlo transparente/oscuro (opacity ~0.25-0.35, transparent:true, o usa transmission si usas MeshPhysicalMaterial) para que se vean los TRONCOS detrás.
   - TRONCOS: material oscuro carbón (~#3a332c) que se ilumine en cálido con la luz de la llama.
   Identifica cada pieza por el nombre de nodo del GLB (frame_*, glass, log_*, foot_*, mist_slot) al recorrer la escena con traverse().

3) LLAMA + NIEBLA saliendo de la RANURA SUPERIOR:
   - Coloca la llama procedural y las partículas de niebla justo encima de la ranura superior del modelo (nodo "mist_slot", centro-arriba). La niebla/llama debe nacer de ahí, no flotar en un punto aleatorio.
   - MANTÉN las partículas sutiles actuales (las que ya gustan). Sobre ellas, añade una llama pequeña, cálida y CONTROLADA (degradado #FFE7C4→#F0824E→#E8674C), con parpadeo suave. IMPORTANTE: pequeña y sin blowout — nada de bola de fuego blanca (ya pasó una vez). Que sea un resplandor cálido elegante, no un incendio.

4) LUZ: una point light cálida (color #F0824E) situada dentro/encima de la ranura, que ilumine el interior (troncos + cristal) y el frente del cuerpo con un brillo cálido. Ambient tenue. Opcional rim light por detrás para separar del fondo #141019.

5) MOVIMIENTO: rotación idle lenta del difusor; al hacer scroll, rota un poco más y la cámara hace un dolly suave (siempre con lerp/damp, nunca set directo). Ease custom con nombre.

6) BLOOM: ajústalo para que resplandezca solo la llama y el brillo cálido interior, sin quemar a blanco. Threshold alto.

──────────────────────────────────────────
NO TOQUES
──────────────────────────────────────────
Copy, títulos, precio, CTA (checkout de Shopify), secciones, footer, scroll suave (Lenis), logo del hero.

RESTRICCIONES:
- Vanilla JS + Vite + Three.js (usa GLTFLoader de examples/jsm). Sin React.
- NO uses el navegador para verificar — reporta y yo verifico con capturas.
- `rm -rf dist && npm run build`, reporta errores.
- Commit al terminar: "hero: cargar difusor .glb real + materiales premium + llama desde la ranura".

REPORTA: parámetros clave que pueda tunear rápido (escala del modelo, tamaño/intensidad de la llama, opacidad del cristal, intensidad de la point light, bloom threshold/strength).
