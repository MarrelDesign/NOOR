PROYECTO: "noor" (ya existe). El difusor .glb ya se carga en el hero, pero hay 3 problemas a arreglar. NO toques copy, secciones, CTA (checkout Shopify), footer ni el scroll suave (Lenis). Confirma git limpio; si no, commitea antes.

PROBLEMAS OBSERVADOS (en pantalla):
1. El difusor NO SE VE: es negro sobre fondo #141019 casi negro y se pierde. Falta iluminación/reflejos que revelen su forma.
2. Queda demasiado abajo y tapado por el titular y el CTA.
3. La "llama" salió como un CONO NARANJA DURO (parece un cono de tráfico), no una llama.

──────────────────────────────────────────
ARREGLOS
──────────────────────────────────────────
1) HACER VISIBLE EL DIFUSOR (lo más importante) — un objeto negro necesita reflejos para leerse:
   - Añade un ENVIRONMENT de reflejos SIN archivos externos: importa RoomEnvironment de 'three/examples/jsm/environments/RoomEnvironment.js' y PMREMGenerator; genera el envMap y asígnalo a scene.environment (NO como background, el fondo sigue siendo #141019).
   - Sube la reflectividad del cuerpo para que capte esos reflejos: MeshStandardMaterial del cuerpo con metalness ~0.4, roughness ~0.35, color #1c1b1f. Ahora el negro brillará en los bordes y se verá la forma.
   - Añade una KEY LIGHT direccional cálida desde arriba-frente (intensidad moderada) y una RIM LIGHT (luz de contra) fría/neutra detrás del difusor para recortar su silueta contra el fondo. Debe leerse CLARAMENTE como un objeto sólido premium, no desaparecer.

2) COMPOSICIÓN — que el difusor sea el protagonista VISIBLE:
   - Súbelo y encuádralo para que se vea entero y con aire (ajusta MODEL.height, su posición Y y/o la posición de la cámara). Que NO quede tapado por el CTA ni el titular.
   - Puede ir detrás del texto, pero tiene que verse el producto. Si hace falta, baja un poco la cámara o sube el modelo para centrarlo en el hero.

3) ARREGLAR LA LLAMA (ahora es un cono duro):
   - Sustituye ese cono por una llama/vapor SUAVE: un sprite o plano con gradiente radial suave y transparencia real (blending additive), flicker orgánico, saliendo de la ranura superior (nodo mist_slot). Bordes difuminados que se disipan hacia arriba, NUNCA un cono sólido de borde nítido.
   - Pequeña y CONTROLADA — sin bola blanca ni cono naranja. Mejor sutil y elegante.
   - MANTÉN las partículas de niebla sutiles que ya gustan.

4) BLOOM: ajústalo para que brille solo el calor de la llama y los reflejos cálidos, sin quemar a blanco y sin cono duro. Threshold alto.

──────────────────────────────────────────
NO TOQUES
──────────────────────────────────────────
Copy, títulos, precio, CTA, secciones, footer, scroll suave, logo del hero.

RESTRICCIONES:
- Vanilla JS + Vite + Three.js. Sin React. No uses el navegador para verificar — reporta y yo verifico con capturas.
- `rm -rf dist && npm run build`, reporta errores.
- Commit: "hero: difusor visible con environment de estudio + rim light + llama suave".

REPORTA: parámetros para tunear rápido (intensidad key/rim light, metalness/roughness del cuerpo, MODEL.height/posición, tamaño/opacidad de la llama, bloom threshold/strength).
